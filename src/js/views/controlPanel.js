import React, { useEffect, useContext } from "react";
import { Link } from "react-router-dom";
import { Context } from "../store/appContext";

export const ControlPanel = () => {
	return <h1>Hola Mundo, esta es la vista de panel de control</h1>;
};
